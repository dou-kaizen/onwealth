# @boilerplate/database — drizzle-kit environment variables
# Copy this file to .env and set DATABASE_URL before running any db:* scripts.

# ── REQUIRED ────────────────────────────────────────────────────────────────

# PostgreSQL connection string (postgresql:// scheme required)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/boilerplate"
